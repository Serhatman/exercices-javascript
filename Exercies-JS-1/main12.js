function squareMaker() {
    let number = document.getElementById ("nombre").value;
    let result = number * number ;

    alert("Le carré de " + number + " " + "est " + result);
    console.log (number)
}